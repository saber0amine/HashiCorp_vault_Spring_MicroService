spring:
  application:
    name: inventory-service
  cloud:
    vault:
      uri: http://vault_container:8200
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: secret
        default-context: inventory-service