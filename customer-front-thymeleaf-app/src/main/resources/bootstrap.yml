spring:
  application:
    name: customer-app
  cloud:
    vault:
      uri: http://vault_container:8200
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: secret
        default-context: customer-app